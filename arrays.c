# include studio.h>

int main () {
    /*
    Syntax
    datatype arrayName[arraySize]*/

    int age[5];

    //storing data in an array 
     int val[4] = {1, 4, 6, 7}; //these values are called array elements


    int items[4] = {2,4};
    //here we had defined the array size to have elements, but have only put three. What will happen here is these C will assign 0 twice to fill the size ealier allocated.

}