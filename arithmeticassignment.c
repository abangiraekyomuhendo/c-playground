#include <stdio.h>

int main() {

    /*
    Let's say you want to print out:
    total = total + number;
    ...you can shorten it to be 
    total += number;

    here is a list off arithmetic assignments
    Addition: += | a += 3 replaces a = a + 3
    Subtraction: -=
    Multiplication: *=
    Division: /=
    Remainder(modulus): %=
    */

    return 0;
}